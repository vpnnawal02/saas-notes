import SignupForm from "./SignupForm";

export const metadata = {
    title: "Sign Up | MyApp",
    description: "Create an account to get started with MyApp.",
};

export default function SignupPage() {
    return (
        <SignupForm />
    );
}
